/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: "/quilibrium-space",
    //output: "export",  // <=== enables static exports
    reactStrictMode: true,
};
    
export default nextConfig;
